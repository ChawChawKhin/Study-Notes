
coordinates = (4, 5)
# tuple is immutable
# coordinates[1] = 10 would result in error
print(coordinates[1])

# Difference between tuple and list
# mutable and immutable
coordinates = [(4, 5), (6, 7), (80, 34)]
# tuple can't be changed
# list can be modify
